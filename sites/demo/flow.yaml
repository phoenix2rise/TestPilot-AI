base_url: "https://example.com"
flows:
  login:
    steps:
      - goto: "/"
      - fill:
          field: "username"
          value: "${USER}"
      - fill:
          field: "password"
          value: "${PASS}"
      - click:
          field: "submit"
      - expect:
          url_contains: "/"
