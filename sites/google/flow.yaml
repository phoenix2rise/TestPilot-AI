base_url: "https://www.google.com"
flows:
  search:
    steps:
      - goto: "/search?q=${QUERY}"
      - click_if_present:
          field: "cookie_reject"
      - expect_visible:
          field: "search_results"
